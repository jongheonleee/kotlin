package book.part5

// Nothing 타입으로 선언하면 이곳에는 null만 대입할 수 있음
// Nothing은 값이 없다는 것을 명시적으로 표현할 때 사용
// 특정 함수가 무언가를 절대 반환하지 않거나 반환하더라도 프로그램에서 의미 있는 데이터가 아니라는 것을 명시적으로 표현하기 위해 사용


// 제네릭에서 Nothing 타입 이용
    // null 이외에 다른 어떤 타입의 데이터도 Nothing 타입에 대입할 수 없음
    // 반대로 Nothing 타입의 데이터는 다른 어떤 타입에도 대입할 수 있음

// Any가 가장 상위 타입, Nothing이 가장 하위 타입
