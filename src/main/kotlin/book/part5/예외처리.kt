package book.part5

// 런타임 예외처리
    // 실행 시 발생하는 예외처리

// 예외 처리
    // 1. 비정상 종료 x -> 정상적으로 대응
    // 2. 강제로 예외 발생

// 예외 처리 : 자바와 마찬가지로 try-catch-finally 구문 활용

// 예외 던지기 : 자바와 마찬가지로 throw 키워드 활용
    // 코틀린에서는 throw는 일종의 표현식 throw 구문을 프로퍼티에 대입 가능
    // throw 만을 반환하는 함수라면 반환 타입을 Nothing으로 지정 가능

// 코틀린에는 throws가 없음
