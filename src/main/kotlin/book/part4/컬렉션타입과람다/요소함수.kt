package book.part4.컬렉션타입과람다


// contains() : 데이터 포함 여부
// elementAt() : 특정 위치의 데이터 추출
// elementAtOrElse() : 인덱스의 데이터를 추출, IndexOutOfBoundsException 발생 x
// elementAtOrNull() : elementAt() + 범위 벗어남 -> null

// first() : 조건에 맞는 첫 번째 데이터, 없으면 NoSuchElementException 발생
// firstOrNull() : first(), 없으면 null
// last() : 조건에 맞는 마지막 데이터
// lastOrNull() : last(), 없으면 null

// indexOf(n) : n의 첫 번째 인덱스 값 반환
// indexOfFirst(lambda) : 조건을 만족하는 데이터의 첫 번재 인덱스 값 반환
// indexOfLast(lambda) : 조건을 만족하는 데이터의 마지막 인덱스 값 반환


