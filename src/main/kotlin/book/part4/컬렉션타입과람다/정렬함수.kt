package book.part4.컬렉션타입과람다

// reversed() : 역정렬(역순)
// sorted() : 정렬(오름차순)
// sortedBy(lambda) : lambda의 결과값으로 정렬(오름차순)
// sortedDescending() : 역정렬(역순)
// sortedDescendingBy() : lambda의 결과값으로 역정렬(내림차순)
